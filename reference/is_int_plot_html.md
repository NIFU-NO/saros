# Detect if data is from int_plot_html

Checks if a data frame has the structure expected from int_plot_html
output, which uses .value column instead of .category column.

## Usage

``` r
is_int_plot_html(data)
```

## Arguments

- data:

  Data frame to check

## Value

Logical indicating if this is int_plot_html format
