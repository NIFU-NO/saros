# Validate integerish parameter

Validate integerish parameter

## Usage

``` r
validate_integerish(
  x,
  min = -Inf,
  max = Inf,
  null_allowed = FALSE,
  call = rlang::caller_env(),
  arg = rlang::caller_arg(x)
)
```
