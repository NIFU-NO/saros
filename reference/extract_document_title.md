# Extract Document Title from File

Internal helper to extract title metadata from DOCX, PPTX, and PDF
files.

## Usage

``` r
extract_document_title(filepath)
```

## Arguments

- filepath:

  Path to the file.

## Value

Character string with the document title, or filename if extraction
fails.
