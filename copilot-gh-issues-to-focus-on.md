# NA

524 cat_table\_\* should consistently display 0, NA and “” as the same
cell Narrow scope — can inspect cat_table\_\* rendering logic and
normalize these values. 493 Whitelist direct column sorting: docs +
validation Clear criteria, but “supported sort keys” need to be defined
by maintainer. 494 Variability-based dep sorting (range of proportions)
New feature with criteria, but needs API naming decision (sort_dep_by =
‘.range’?). 492 Support indep totals ordering Complex feature, needs
materialization-point decision (upstream vs on-demand).
