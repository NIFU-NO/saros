% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_quarto_website_index.R
\name{check_quarto_website_index}
\alias{check_quarto_website_index}
\title{Check Quarto Website Folders for Missing index.qmd}
\usage{
check_quarto_website_index(path = ".", quiet = FALSE)
}
\arguments{
\item{path}{\emph{Path to project root}

\verb{scalar<character>} // default: \code{"."} (\code{optional})

The root directory of the Quarto website project to check.}

\item{quiet}{\emph{Suppress warnings}

\verb{scalar<logical>} // default: \code{FALSE} (\code{optional})

If \code{TRUE}, no cli warnings are issued. The affected paths are still
returned invisibly.}
}
\value{
A character vector of folder paths (relative to \code{path}) that
contain \code{.qmd} files but lack an \code{index.qmd}. Returned invisibly.
}
\description{
Scans a Quarto website project directory for folders that contain \code{.qmd}
files (directly or in subfolders) but are missing an \code{index.qmd} file.
Such folders often cause a malfunctioning navigation menu in the rendered
Quarto website.

Folders whose names start with \verb{_} or \code{.} are excluded, as these are
typically Quarto internal or hidden directories.
}
\examples{
\dontrun{
# Check the current project
check_quarto_website_index()

# Check a specific directory
check_quarto_website_index("path/to/quarto-project")
}
}
