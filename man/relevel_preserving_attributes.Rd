% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{relevel_preserving_attributes}
\alias{relevel_preserving_attributes}
\title{Reorder a factor's levels without losing its other attributes}
\usage{
relevel_preserving_attributes(x, levels)
}
\arguments{
\item{x}{A factor or vector to reorder}

\item{levels}{Desired level order. Observed values it does not mention keep
their place at the end rather than becoming \code{NA}.}
}
\value{
\code{x} with its levels reordered and its attributes intact
}
\description{
\code{factor()} returns a value carrying only \code{levels} and \code{class}, so it
silently drops the variable \code{label} that \code{get_raw_labels()} and the plot
titles read. Everything \code{factor()} does not set itself is copied back.
}
\keyword{internal}
