% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sorting_utils.R
\name{get_indep_level_order}
\alias{get_indep_level_order}
\title{Read the intended order of the independent variable's categories}
\usage{
get_indep_level_order(data_summary, data, indep)
}
\arguments{
\item{data_summary}{Summarized data, possibly carrying \code{.indep_order}}

\item{data}{Raw dataset, used for the factor-level fallback}

\item{indep}{Name of the independent variable, or \code{NULL}}
}
\value{
Character vector of category labels in order, or \code{NULL}
}
\description{
Content types that build their own tables rather than arranging
\code{data_summary} still need the category order the rest of the package
settled on. \code{.indep_order} is the authoritative source, because
\code{add_indep_order()} has already resolved \code{sort_indep_by} and
\code{descend_indep} into it. It is only present for categorical dependent
variables; \code{summarize_int_cat_data()} does no sorting, so a numeric
dependent variable falls back to the factor's own levels -- which is what
\code{arrange_table_data()} does in the same situation.
}
\keyword{internal}
